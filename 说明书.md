# 基于AI视觉的汽车白车身焊点质量检测系统

**项目说明书**
《智能生产与制造服务技术》课程项目 · 第13组

---

## 目录

1. [项目概述](#1-项目概述)
2. [系统架构](#2-系统架构)
3. [环境搭建](#3-环境搭建)
4. [数据集说明](#4-数据集说明)
5. [模型训练](#5-模型训练)
6. [模型评估](#6-模型评估)
7. [演示系统](#7-演示系统)
8. [部署到云端](#8-部署到云端)
9. [项目文件结构](#9-项目文件结构)
10. [技术栈](#10-技术栈)
11. [常见问题](#11-常见问题)

---

## 1. 项目概述

### 1.1 项目背景

在汽车制造过程中，白车身（Body in White, BIW）的焊点质量直接关系到整车的结构强度、安全性和耐久性。传统的焊点检测主要依赖人工目视检查，存在效率低、标准不统一、容易漏检等问题。

本项目基于 **YOLOv8** 目标检测算法，结合合成数据生成技术，构建了一套自动化的焊点缺陷检测系统，实现对5类焊点状态的实时识别与分类。

### 1.2 检测目标

| 类别 | 标签 | 说明 |
|------|------|------|
| ✅ 合格焊点 | good | 焊点饱满、熔合良好、表面光滑 |
| ⚠️ 冷焊（虚焊） | cold_weld | 熔合不充分，焊点暗淡或不完整 |
| 🔥 过烧 | overburn | 焊接温度过高，焊点烧蚀或飞溅 |
| 💥 裂纹 | crack | 焊点表面或边缘出现裂纹 |
| 🕳️ 缩孔 | shrinkage | 焊点表面凹陷或出现气孔 |

### 1.3 项目特点

- **合成数据驱动**：无需大量真实标注数据，通过程序自动生成多样化焊点图像
- **端到端流程**：从数据生成到模型部署一站式完成
- **交互式演示**：基于 Streamlit 的可视化界面，支持上传图片实时检测
- **云端可部署**：支持部署到 Streamlit Cloud，团队共享使用

---

## 2. 系统架构

```
┌─────────────────────────────────────────────────────┐
│                     数据层                          │
│  01_generate_data.py → 合成焊点图像（2000张）       │
│                      ↓                              │
│                   数据集                            │
│         train/1407张 | val/292张 | test/301张        │
├─────────────────────────────────────────────────────┤
│                     模型层                          │
│  02_train.py → YOLOv8n 训练（50 Epochs）            │
│                  ↓                                  │
│            best.pt（训练好的权重）                   │
├─────────────────────────────────────────────────────┤
│                     评估层                          │
│  03_evaluate.py → 混淆矩阵 + 分类报告 + 检测可视化   │
├─────────────────────────────────────────────────────┤
│                     应用层                          │
│  04_demo.py → Streamlit 交互式演示系统              │
│              ↓                                      │
│        本地: http://localhost:8501                   │
│        云端: Streamlit Cloud 部署                   │
└─────────────────────────────────────────────────────┘
```

---

## 3. 环境搭建

### 3.1 系统要求

- **操作系统**: Windows 10/11, macOS, Linux
- **Python**: 3.9 ~ 3.12
- **内存**: 推荐 8GB 以上（CPU 训练需要）
- **磁盘**: 至少 5GB 可用空间
- **GPU（可选）**: NVIDIA 显卡 + CUDA（可大幅加速训练）

### 3.2 安装步骤

#### 方式一：Git 克隆
```bash
git clone https://github.com/thompaihwagners-collab/weld_inspection.git
cd weld_inspection
```

#### 方式二：直接下载 ZIP
1. 打开 https://github.com/thompaihwagners-collab/weld_inspection
2. 点击绿色按钮 **<> Code** → **Download ZIP**
3. 解压到本地目录

#### 安装依赖
```bash
pip install -r requirements.txt
```

`requirements.txt` 包含以下主要依赖：
- `ultralytics` — YOLOv8 目标检测框架
- `streamlit` — 交互式演示框架
- `opencv-python-headless` — 图像处理
- `pillow` — 图像处理
- `numpy` — 数值计算
- `matplotlib` + `seaborn` — 数据可视化
- `scikit-learn` — 评估指标

### 3.3 验证安装
```bash
python -c "from ultralytics import YOLO; print('YOLOv8 ready')"
python -c "import streamlit; print('Streamlit ready')"
```

---

## 4. 数据集说明

### 4.1 数据生成

运行以下命令生成 2000 张合成焊点图像：
```bash
python 01_generate_data.py
```

生成的图像包含以下特征：
- **图像尺寸**: 640 × 640 像素
- **标注格式**: YOLO 格式（txt 文件，每行：`class_id x_center y_center width height`）
- **数据划分**: 训练集 1407 张 / 验证集 292 张 / 测试集 301 张

### 4.2 合成逻辑

每张焊点图像通过以下方式合成：
1. 随机生成金属背景纹理（高斯噪声 + 滤波）
2. 在随机位置绘制焊点（不同大小、形状）
3. 根据类别添加缺陷特征：
   - 冷焊：焊接区域暗淡、不完整
   - 过烧：烧蚀痕迹、飞溅光晕
   - 裂纹：焊点边缘随机裂纹线
   - 缩孔：焊点中心凹陷、气孔
4. 添加光照变化和随机噪声增强多样性

### 4.3 数据集结构
```
output/dataset/
├── train/
│   ├── images/     # 1407 张训练图像
│   └── labels/     # 1407 个标注文件
├── val/
│   ├── images/     # 292 张验证图像
│   └── labels/     # 292 个标注文件
└── test/
    ├── images/     # 301 张测试图像
    └── labels/     # 301 个标注文件
└── data.yaml      # 数据集配置文件
```

---

## 5. 模型训练

### 5.1 启动训练
```bash
python 02_train.py
```

### 5.2 训练参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 模型 | yolov8n.pt | YOLOv8 Nano（轻量级） |
| 训练轮数 | 50 | 根据loss收敛情况可调整 |
| 批次大小 | 4-16 | 根据显存/内存自动调整 |
| 图像尺寸 | 640×640 | 标准YOLO输入尺寸 |
| 优化器 | AdamW | 自适应学习率 |
| 早停 | 30轮无改善 | 自动防止过拟合 |

### 5.3 训练过程

程序会自动检测 GPU（CUDA）并优先使用：
- **GPU 模式**：训练速度约快 5-10 倍
- **CPU 模式**：稳妥训练，不会因显存不足报错

训练时会输出每个 Epoch 的指标：
```
Epoch    GPU_mem   box_loss   cls_loss   dfl_loss
 1/50      0G      0.8325     0.9549     0.8885
 7/50      0G      0.1975     0.3242     0.7204
12/50      0G      0.2021     0.3270     0.8088
...
```

### 5.4 训练结果

训练完成后，模型权重保存在：
```
output/runs/weld_detection/weights/
├── best.pt    # 最佳模型（验证集指标最优）
└── last.pt    # 最后一轮模型
```

**训练指标摘要**
- 测试集 mAP50：99.5%
- 测试集 Precision：99.9%
- 测试集 Recall：100%
- 5 个缺陷类别 F1-Score 均为 1.00

> ⚠️ **注意**：上述高指标是由于使用合成数据训练的结果。真实工业场景中的焊点检测需要更复杂的特征工程和更多真实数据支持。

---

## 6. 模型评估

### 6.1 运行评估
```bash
python 03_evaluate.py
```

### 6.2 评估输出

评估脚本会生成以下内容：

| 文件 | 路径 | 说明 |
|------|------|------|
| 混淆矩阵 | `output/figures/confusion_matrix.png` | 5类预测结果分布 |
| 检测示例 | `output/figures/detection_examples.png` | 测试集随机样本检测结果 |
| 评估摘要 | `output/figures/evaluation_summary.png` | 综合指标卡片 |
| 分类报告 | 终端输出 | 每类的Precision/Recall/F1 |

### 6.3 评估指标说明

- **Precision（精确率）**：模型预测为正类中，实际为正类的比例
- **Recall（召回率）**：实际为正类的样本中，被正确预测的比例
- **mAP50（平均精度 @IoU=0.5）**：IoU阈值0.5时的平均精度，综合衡量检测性能

---

## 7. 演示系统

### 7.1 启动本地演示
```bash
streamlit run 04_demo.py
```
然后在浏览器打开 `http://localhost:8501`

### 7.2 功能模块

演示系统包含 4 个主要功能页面：

#### 页面一：交互式焊点检测
- 上传焊点图片实时检测
- 自动识别缺陷类别和位置
- 显示检测置信度

#### 页面二：批量测试
- 从测试集中随机选取样本
- 批量运行检测并统计各类别结果
- 支持多次测试汇总

#### 页面三：模型评估报告
- 混淆矩阵可视化
- 测试集分类报告
- 检测示例展示

#### 页面四：缺陷类型说明
- 5类焊点特征文字说明
- 检测流程介绍
- 系统技术栈展示

### 7.3 界面布局
```
┌──────────────────────────────────────────────────────┐
│  🔧 基于AI视觉的汽车白车身焊点质量检测系统           │
│  ┌──── 侧边栏 ────┐  ┌──────── 主内容区 ──────────┐ │
│  │ 功能选择       │  │  图片上传/检测结果          │ │
│  │  ○ 焊点检测    │  │  或                          │ │
│  │  ○ 批量测试    │  │  指标卡片                    │ │
│  │  ○ 评估报告    │  │  或                          │ │
│  │  ○ 缺陷说明    │  │  批量结果汇总                │ │
│  └────────────────┘  └──────────────────────────────┘ │
└──────────────────────────────────────────────────────┘
```

---

## 8. 部署到云端

### 8.1 部署到 Streamlit Cloud（免费）

1. 确保代码已推送到 GitHub 仓库
2. 打开 https://streamlit.io/cloud
3. 使用 GitHub 账号登录
4. 点击 **New app**
5. 选择仓库 `thompaihwagners-collab/weld_inspection`
6. 保持默认设置（Branch: main, File: 04_demo.py）
7. 点击 **Deploy**

部署完成后会获得一个公开链接，如 `https://weld-inspection.streamlit.app`，组员可以直接在浏览器中打开使用。

### 8.2 局域网分享

如果只需在局域网内部署：
```bash
streamlit run 04_demo.py --server.address 0.0.0.0
```
终端会显示 `Network URL: http://192.168.x.x:8501`，将该 IP 分享给同一网络下的组员即可访问。

---

## 9. 项目文件结构

```
weld_inspection/
│
├── 01_generate_data.py      # 合成数据集生成器
├── 02_train.py              # YOLOv8模型训练脚本
├── 03_evaluate.py           # 模型评估脚本
├── 04_demo.py               # Streamlit 交互式演示系统
├── requirements.txt         # Python 依赖列表
├── best.pt                  # 训练好的模型权重
├── .gitignore               # Git 忽略规则
├── README.md                # 项目简介
└── 本项目说明书 (.md)       # 本文件
│
├── output/                  # 输出目录（本地生成）
│   ├── dataset/             # 合成数据集
│   │   ├── train/           # 训练集
│   │   ├── val/             # 验证集
│   │   ├── test/            # 测试集
│   │   └── data.yaml        # 数据集配置
│   ├── runs/                # 训练结果
│   │   └── weld_detection/  # 检测模型训练记录
│   └── figures/             # 评估可视化图表
│
└── yolo26n.pt / yolov8n.pt # 预训练基础模型
```

---

## 10. 技术栈

### 核心框架
| 技术 | 用途 | 版本 |
|------|------|------|
| Python | 编程语言 | 3.9+ |
| YOLOv8 (Ultralytics) | 目标检测模型 | 8.0+ |
| PyTorch | 深度学习框架 | 1.8+ |

### 数据处理与可视化
| 技术 | 用途 |
|------|------|
| OpenCV | 图像处理与数据生成 |
| NumPy | 数值计算 |
| Matplotlib | 图表绘制 |
| Seaborn | 统计可视化 |

### 应用部署
| 技术 | 用途 |
|------|------|
| Streamlit | 交互式Web应用 |
| GitHub | 代码托管与版本控制 |
| Streamlit Cloud | 云端部署 |

---

## 11. 常见问题

### Q1: 训练速度很慢怎么办？
- 默认使用 CPU 训练，建议关闭其他程序释放内存
- 如果有 NVIDIA 显卡，安装 CUDA 版 PyTorch：`pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126`

### Q2: 训练时报错 "WinError 1455 页面文件太小"
- 这是 Windows 虚拟内存不足导致 CUDA 无法加载
- 解决方案：设置 → 系统 → 高级系统设置 → 性能 → 虚拟内存 → 自定义大小（初始 16384MB / 最大 32768MB），重启后生效

### Q3: Streamlit 部署后模型未加载
- 确认 `best.pt` 已推送到 GitHub 仓库
- 检查仓库的 `.gitignore` 是否误将 `.pt` 文件排除

### Q4: 如何修改检测类别数量？
- 修改 `01_generate_data.py` 中的缺陷定义
- 重新生成数据集后重新训练

### Q5: 如何让组员也能修改代码？
- 在 GitHub 仓库的 **Settings → Collaborators → Add people** 中添加组员的 GitHub 账号

---

## 附录：运行全流程

```bash
# 1. 克隆仓库
git clone https://github.com/thompaihwagners-collab/weld_inspection.git
cd weld_inspection

# 2. 安装依赖
pip install -r requirements.txt

# 3. 生成数据集（约30秒）
python 01_generate_data.py

# 4. 训练模型（CPU约4-5小时，GPU约30分钟）
python 02_train.py

# 5. 评估模型
python 03_evaluate.py

# 6. 启动演示系统
streamlit run 04_demo.py
# 浏览器打开 http://localhost:8501
```

---

*文档版本：v1.0 · 2026年5月*
*《智能生产与制造服务技术》课程项目 · 第13组*
